import 'package:flutter/material.dart';

const kPlaceHolderColor = Colors.grey;

placeHolder(double width, double height) => Container(
      width: width,
      height: height,
      color: kPlaceHolderColor,
    );
